// import { PrismaClient } from "@prisma/client";

// const prisma = new PrismaClient()

// async function main () {
//   const user = await prisma.user.create({data:{firstName:"Varsha", lastName: "Sharma", profileImageURL:"", email: "varsha1@gmail.com",password:"12345", salt:""} })

// console.log("User", user)
// }

// main().catch((e)=>{console.log("E", e.message)})

